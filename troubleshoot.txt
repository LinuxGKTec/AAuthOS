
Keyclock images pull error:
 

 Warning  Failed     12m (x4 over 13m)     kubelet            Failed to pull image "docker.io/bitnami/keycloak:24.0.5-debian-12-r1": rpc error: code = NotFound desc = failed to pull and unpack image "docker.io/bitnami/keycloak:24.0.5-debian-12-r1": failed to resolve reference "docker.io/bitnami/keycloak:24.0.5-debian-12-r1": docker.io/bitnami/keycloak:24.0.5-debian-12-r1: not found


docker.io/bitnami/postgresql:16.3.0-debian-12-r16": rpc error: code = NotFound desc = failed to pull and unpack image "docker.io/bitnami/postgresql:16.3.0-debian-12-r16": failed to resolve reference "docker.io/bitnami/postgresql:16.3.0-debian-12-r16": docker.io/bitnami/postgresql:16.3.0-debian-12-r16: not found



  Failed to pull image "docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6": rpc error: code = NotFound desc = failed to pull and unpack image "docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6": failed to resolve reference "docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6": docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6: not found


docker pull --platform linux/amd64 public.ecr.aws/bitnami/keycloak-config-cli:5.12.0-debian-12-r6
docker tag public.ecr.aws/bitnami/keycloak-config-cli:5.12.0-debian-12-r6 docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6
kind load docker-image docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6 --name kind
kind get clusters
kubectl get nodes
kind load docker-image docker.io/bitnami/keycloak-config-cli:5.12.0-debian-12-r6 --name aauth-demo



autams-MacBook-Pro:AAuthOS gautamkumar$ kubectl get pod -n idp
NAME                                 READY   STATUS    RESTARTS   AGE
keycloak-0                           1/1     Running   0          102m
keycloak-keycloak-config-cli-fgpkl   1/1     Running   0          99m
keycloak-postgresql-0                1/1     Running   0          102m
Gautams-MacBook-Pro:AAuthOS gautamkumar$ 


Platform error:

 Normal   Scheduled  9m58s                   default-scheduler  Successfully assigned platform/mission-service-5446d4cf7f-w5xkr to aauth-demo-worker
  Warning  Failed     7m47s (x12 over 9m58s)  kubelet            Error: configmap "platform-config" not found
  Normal   Pulled     4m52s (x25 over 9m58s)  kubelet            Container image "aauth/mission-service:dev" already present on machine



Gautams-MacBook-Pro:AAuthOS gautamkumar$ make keycloak
==> [02] Deploying Keycloak into ns/idp
"bitnami" already exists with the same configuration, skipping
configmap/keycloak-realm-import configured
Release "keycloak" does not exist. Installing it now.
NAME: keycloak
LAST DEPLOYED: Tue Jun  9 16:23:27 2026
NAMESPACE: idp
STATUS: deployed
REVISION: 1
DESCRIPTION: Install complete
TEST SUITE: None
NOTES:
CHART NAME: keycloak
CHART VERSION: 21.6.1
APP VERSION: 24.0.5

** Please be patient while the chart is being deployed **

Keycloak can be accessed through the following DNS name from within your cluster:

    keycloak.idp.svc.cluster.local (port 80)

To access Keycloak from outside the cluster execute the following commands:

1. Get the Keycloak URL and associate its hostname to your cluster external IP:

   export CLUSTER_IP=$(minikube ip) # On Minikube. Use: `kubectl cluster-info` on others K8s clusters
   echo "Keycloak URL: http://keycloak.aauth.local/"
   echo "$CLUSTER_IP  keycloak.aauth.local" | sudo tee -a /etc/hosts

2. Access Keycloak using the obtained URL.
3. Access the Administration Console using the following credentials:

  echo Username: admin
  echo Password: $(kubectl get secret --namespace idp keycloak -o jsonpath="{.data.admin-password}" | base64 -d)

WARNING: There are "resources" sections in the chart not set. Using "resourcesPreset" is not recommended for production. For production installations, please set the following values according to your workload needs:
  - keycloakConfigCli.resources
+info https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/

⚠ SECURITY WARNING: Original containers have been substituted. This Helm chart was designed, tested, and validated on multiple platforms using a specific set of Bitnami and Tanzu Application Catalog containers. Substituting other containers is likely to cause degraded security and performance, broken chart features, and missing environment variables.

Substituted images detected:
  - docker.io/bitnamilegacy/keycloak:24.0.5-debian-12-r1
  - docker.io/bitnamilegacy/keycloak-config-cli:5.12.0-debian-12-r6
==> Waiting for Keycloak to be ready
Error from server (NotFound): deployments.apps "keycloak" not found
make: *** [keycloak] Error 1

helm uninstall keycloak -n idp
  593  kubectl delete job keycloak-keycloak-config-cli -n idp --ignore-not-found=true
  594  kubectl delete pvc data-keycloak-postgresql-0 -n idp --ignore-not-found=true
  595  history 



IDP_FLAVOR=keycloak
==> Platform up. Next: scripts/04-deploy-agentgateway.sh
    registry-service: http://localhost:9000
    mission-service:  http://localhost:9001
    operator console: http://localhost:9002  (login: operator / aauth-operator-demo)


Error in make app


autams-MacBook-Pro:AAuthOS gautamkumar$ make apps
==> Cloning https://github.com/christian-posta/aauth-full-demo.git (main)
Cloning into '/Users/gautamkumar/Devops/Kubernates/AAuthOS/.work/aauth-full-demo'...
remote: Enumerating objects: 184, done.
remote: Counting objects: 100% (184/184), done.
remote: Compressing objects: 100% (161/161), done.
remote: Total 184 (delta 27), reused 111 (delta 12), pack-reused 0 (from 0)
Receiving objects: 100% (184/184), 4.61 MiB | 5.70 MiB/s, done.
Resolving deltas: 100% (27/27), done.
==> Applying aauth_sdk patches to upstream
[patched] /Users/gautamkumar/Devops/Kubernates/AAuthOS/.work/aauth-full-demo/backend/app/main.py
[stubbed] /Users/gautamkumar/Devops/Kubernates/AAuthOS/.work/aauth-full-demo/backend/app/services/aauth_interceptor.py
Traceback (most recent call last):
  File "/Users/gautamkumar/Devops/Kubernates/AAuthOS/sdk/integration/patches/apply_patches.py", line 185, in <module>
    sys.exit(main(Path(args.repo_root)))
             ~~~~^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/gautamkumar/Devops/Kubernates/AAuthOS/sdk/integration/patches/apply_patches.py", line 161, in main
    patch_file(
    ~~~~~~~~~~^
        repo_root / "supply-chain-agent" / "__main__.py",
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
        add_boot=True, add_app_wiring=True,
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/Users/gautamkumar/Devops/Kubernates/AAuthOS/sdk/integration/patches/apply_patches.py", line 130, in patch_file
    src = _insert_after_app_construct(src, APP_WIRING_TEMPLATE)
  File "/Users/gautamkumar/Devops/Kubernates/AAuthOS/sdk/integration/patches/apply_patches.py", line 106, in _insert_after_app_construct
    raise RuntimeError("could not find `app = FastAPI(...)` to anchor wiring")
RuntimeError: could not find `app = FastAPI(...)` to anchor wiring
make: *** [apps] Error 1


Some issue in function 

def _insert_after_app_construct(src: str, block: str) -> str:
    """Insert `block` after the first `app = FastAPI(...)` line."""
    if block in src:
        return src
    pattern = re.compile(r"^app\s*=\s*FastAPI\s*\(.*?\)\s*$", re.MULTILINE | re.DOTALL)
    m = pattern.search(src)
    if not m:
        # Fallback: look for `app = FastAPI(` and find the matching `)`.
        idx = src.find("app = FastAPI(")
        if idx < 0:
            raise RuntimeError("could not find `app = FastAPI(...)` to anchor wiring")
        depth = 0
        end = idx
        for i in range(idx, len(src)):
            if src[i] == "(":
                depth += 1
            elif src[i] == ")":
                depth -= 1
                if depth == 0:
                    end = i + 1
                    break
        return src[:end] + "\n\n" + block + "\n" + src[end:]
    return src[: m.end()] + "\n\n" + block + "\n" + src[m.end():]


Proposed:

def _insert_after_app_construct(src: str, block: str) -> str:
    """Insert `block` after the ASGI/FastAPI application initialization anchor."""
    if block in src:
        return src

    # Target lines like: app = FastAPI(...), app = server.build(), app = Starlette(...)
    anchors = ["= FastAPI(", "= server.build(", "= Starlette("]
    idx = -1
    matched_anchor = ""
    
    # Normalize spaces inside the search array to ensure we catch minor formatting variations
    normalized_src = src.replace(" ", "")
    
    for anchor in anchors:
        norm_anchor = anchor.replace(" ", "")
        if norm_anchor in normalized_src:
            # Find where it actually sits in the original source code string
            # We look for the variable assignment leading to the constructor
            import re
            match = re.search(r"app\s*=\s*" + re.escape(anchor.split("=")[1].strip()), src)
            if match:
                idx = match.start()
                matched_anchor = anchor
                break

    if idx < 0:
        raise RuntimeError("could not find an application initialization anchor (`app = ...`) to hook wiring")

    # Find the end of the statement line or its closing parenthesis bounds
    depth = 0
    end = idx
    for i in range(idx, len(src)):
        if src[i] == "(":
            depth += 1
        elif src[i] == ")":
            depth -= 1
            if depth == 0:
                end = i + 1
                break
        elif src[i] == "\n" and depth == 0:
            # If it's a direct function call without long wrapped tuples, break at the newline
            end = i
            break
            
    return src[:end] + "\n\n" + block + "\n" + src[end:]



local df="${WORK}/aauth-full-demo/${subdir}/Dockerfile"
  # FIX: Reset the Dockerfile to its clean git state before patching
  (cd "${WORK}/aauth-full-demo" && git checkout -- "${subdir}/Dockerfile")
  
  if ! grep -q "aauth-sdk" "${df}"; then
    # Inject before the CMD so the install happens early enough to fail fast.
    awk -v sd="${subdir}" '



> CANCELED [ 3/10] RUN apt-get update && apt-get install -y     curl     && rm -rf /var/lib/apt/  0.3s
 => CACHED [ 4/10] RUN pip install --no-cache-dir uv                                                0.0s
 => CACHED [ 5/10] COPY backend/pyproject.toml backend/uv.lock ./                                   0.0s
 => CACHED [ 6/10] RUN uv pip install --system --requirement pyproject.toml                         0.0s
 => CACHED [ 7/10] COPY backend/ .                                                                  0.0s
 => CACHED [ 8/10] RUN useradd --create-home --shell /bin/bash app &&     chown -R app:app /app     0.0s
 => ERROR [ 9/10] COPY aauth-sdk /opt/aauth-sdk                                                     0.0s
------
 > [ 9/10] COPY aauth-sdk /opt/aauth-sdk:
------
failed to compute cache key: "/aauth-sdk" not found: not found
make: *** [apps] Error 1


Today RND:
Keycloak can be accessed through the following DNS name from within your cluster:

    keycloak.idp.svc.cluster.local (port 80)

To access Keycloak from outside the cluster execute the following commands:

1. Get the Keycloak URL and associate its hostname to your cluster external IP:

   export CLUSTER_IP=$(minikube ip) # On Minikube. Use: `kubectl cluster-info` on others K8s clusters
   echo "Keycloak URL: http://keycloak.aauth.local/"
   echo "$CLUSTER_IP  keycloak.aauth.local" | sudo tee -a /etc/hosts

2. Access Keycloak using the obtained URL.
3. Access the Administration Console using the following credentials:

  echo Username: admin
  echo Password: $(kubectl get secret --namespace idp keycloak -o jsonpath="{.data.admin-password}" | base64 -d)


==> Platform up. Next: scripts/04-deploy-agentgateway.sh
    registry-service: http://localhost:9000
    mission-service:  http://localhost:9001
    operator console: http://localhost:9002  (login: operator / aauth-operator-demo)


Make gateway:

error: Internal error occurred: error executing command in container: failed to exec in container: failed to start exec "f65f5cb5a7bf70e5fc6f2c551de82870bb9f7ae9d69c8aec358fff518abe44d5": OCI runtime exec failed: exec failed: unable to start container process: exec: "curl": executable file not found in $PATH: unknown
make: *** [gateway] Error 1

To fix above error we need to install curl in container
kubectl exec -it deploy/registry-service -n platform -- sh -c "apt-get update && apt-get install -y curl"


Gautams-MacBook-Pro:AAuthOS gautamkumar$ make register
==> Registering market-analysis
    market-analysis → https://platform.aauth.local/agents/market-analysis
==> Registering supply-chain
    supply-chain → https://platform.aauth.local/agents/supply-chain
==> Registering backend
    backend → https://platform.aauth.local/agents/backend
==> All agents pre-registered.
    Bootstrap tokens written to /Users/gautamkumar/Devops/Kubernates/AAuthOS/.bootstrap-tokens (gitignored).
    Next: scripts/06-deploy-apps.sh

In apps deployment,  I have fixed three Dockerfile for backend, supply-chain-agent, market-analysis-agent
And make apps build three images successfully and loaded in kind cluster but next task “Building frontend (no SDK — UI is not a registered agent)” failed with error:

==> Building frontend (no SDK — UI is not a registered agent)
[+] Building 0.3s (2/2) FINISHED                                                                                                      
 => [internal] load build definition from Dockerfile                                                                             0.2s
 => => transferring dockerfile: 2B                                                                                               0.0s
 => [internal] load .dockerignore                                                                                                0.2s
 => => transferring context: 2B                                                                                                  0.0s
failed to solve with frontend dockerfile.v0: failed to read dockerfile: open /var/lib/docker/tmp/buildkit-mount376593463/Dockerfile: no such file or directory
make: *** [apps] Error 1

I checked script found following code in which make apps is failing 

echo "==> Building frontend (no SDK — UI is not a registered agent)"
docker build -t aauth/supply-chain-ui:dev "${WORK}/aauth-full-demo/supply-chain-ui"
kind load docker-image aauth/supply-chain-ui:dev --name "${CLUSTER_NAME}"

As there is not Dockerfile for under supply-chain-ui directory in repo of christian-posta/aauth-full-demo

Gautams-MacBook-Pro:AAuthOS gautamkumar$ cd .work/aauth-full-demo/supply-chain-ui/

Gautams-MacBook-Pro:supply-chain-ui gautamkumar$ tree
.
├── env.example
├── env.template
├── package.json
├── postcss.config.js
├── public
│   └── index.html
├── src
│   ├── api.js
│   ├── App.css
│   ├── App.js
│   ├── components
│   │   ├── MarkdownRenderer.js
│   │   └── MarkdownTest.js
│   ├── hooks
│   │   └── useOptimization.js
│   ├── index.css
│   ├── index.js
│   ├── reportWebVitals.js
│   └── utils
│       └── markdownUtils.js
└── tailwind.config.js

So to proceed further we need to put Dockerfile under supply-chain-ui


# Before
def _insert_after_app_construct(src: str, block: str) -> str:
    """Insert `block` after the first FastAPI construction, preserving scope."""
    src = _strip_app_wiring(src)
    pattern = re.compile(

# After
def _insert_after_app_construct(src: str, block: str) -> str:
    """Insert `block` after the first FastAPI construction, preserving scope."""
    # Check if any rendered variant is already present before stripping
    start_marker = "# ---- aauth_sdk app wiring (added by apply_patches.py) ----"
    if start_marker in src:
        return src
    src = _strip_app_wiring(src)
    pattern = re.compile(


